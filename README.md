# Use IBM Watson speech to text API

- credential:

```json
{
  "url": "https://stream.watsonplatform.net/speech-to-text/api",
  "username": "xxxxx",
  "password": "xxxxx"
}
```

- input is in `audio` folder (wav)
- output is in `text` folder (txt)
